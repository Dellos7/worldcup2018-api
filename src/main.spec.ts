import DataService from './main';
import {expect, assert} from 'chai';

describe('Main', () => {

    describe('initFromJsonDataUrl', () => {
        it('should get json data from url', () => {
            /*let jsonDataUrl: string = 'https://raw.githubusercontent.com/lsv/fifa-worldcup-2018/master/data.json';
            DataService.initFromJsonDataUrl(jsonDataUrl);*/
        });
    });

});